{
  "name": "cannedlatte",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "prebuild": "npm run check:data",
    "postbuild": "npm run check:claims && npm run check:urls",
    "check:data": "node scripts/check-data.mjs",
    "check:claims": "node scripts/check-claims.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "check:urls": "node scripts/check-urls.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.4.0",
    "astro": "^5.12.0"
  }
}
